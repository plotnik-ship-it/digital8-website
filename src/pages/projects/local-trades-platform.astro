---
import BaseLayout from '../../layouts/BaseLayout.astro';

const techStack = ["PWA", "Vue.js", "Firebase"];

const keyFeatures = [
  "Instant Quote Generation",
  "Mobile-First Design for Field Work",
  "Real-Time Client Communication",
];
---

<BaseLayout title="Case Study: Platform for Local Trades" pageDescription="A mobile-first PWA that allows field workers to send quotes in under 60 seconds, increasing win-rates.">
  <style>
    body {
      background-color: #050505;
    }
  </style>

  <main class="py-24 px-4 text-off-white">
    <div class="max-w-4xl mx-auto">
      
      <!-- Header -->
      <div class="text-center mb-16">
        <h1 class="text-5xl md:text-7xl font-bold clash-display" style="color: #e1fe3f;">Platform for Local Trades</h1>
        <p class="text-xl md:text-2xl text-gray-300 mt-4 max-w-3xl mx-auto">A mobile-first PWA that allows field workers to send quotes in under 60 seconds, increasing win-rates.</p>
      </div>

      <!-- Main Image -->
      <div class="mb-16">
        <img src="/localtrades-full.webp" alt="Local Trades Quoting Platform" class="w-full rounded-2xl shadow-2xl shadow-electric-yellow/10 border border-white/10">
      </div>

      <!-- Tech Stack -->
      <div class="flex justify-center gap-3 mb-16 flex-wrap">
        {techStack.map(tag => (
          <span class="bg-white/10 text-off-white/80 text-sm font-semibold px-4 py-2 rounded-full">{tag}</span>
        ))}
      </div>

      <!-- Main Content -->
      <div class="prose prose-invert prose-lg max-w-none mx-auto text-gray-300">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-16">
          <div>
            <h2 class="text-3xl font-bold clash-display text-off-white">The Problem</h2>
            <p>Tradespeople and contractors often rely on pen-and-paper notes or slow, cumbersome desktop software to create and send quotes. This process is inefficient, prone to errors, and can lead to lost business if a potential client has to wait hours or days for a price.</p>
          </div>
          <div>
            <h2 class="text-3xl font-bold clash-display text-off-white">The Solution</h2>
            <p>We built a Progressive Web App (PWA) that works flawlessly on any phone, tablet, or desktop. It allows field workers to generate and send professional, accurate quotes directly from the job site in under a minute, drastically improving their ability to win bids.</p>
            <ul class="mt-6 space-y-4">
              {keyFeatures.map(feature => (
                <li class="flex items-start">
                  <svg class="w-6 h-6 text-green-400 mr-3 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path></svg>
                  <span>{feature}</span>
                </li>
              ))}
            </ul>
          </div>
        </div>
      </div>

      <!-- CTA -->
      <div class="text-center mt-24">
        <a href="/#contact" class="group relative px-10 py-5 border border-electric-yellow rounded-full overflow-hidden inline-block transition-all text-electric-yellow btn-glow font-bold text-lg">
          <span class="relative z-10 group-hover:text-brand-dark transition-colors duration-300">Discuss This Solution</span>
          <div class="absolute inset-0 bg-electric-yellow translate-y-full group-hover:translate-y-0 transition-transform duration-300 ease-out"></div>
        </a>
      </div>

    </div>
  </main>
</BaseLayout>
