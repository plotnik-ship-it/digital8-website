---
import BaseLayout from '../../layouts/BaseLayout.astro';

const techStack = ["PWA", "Vue.js", "Firebase", "Geolocation"];

const keyFeatures = [
  "Instant Quote Generation: Field workers can create and send professional PDF quotes from their mobile devices in under 60 seconds.",
  "Job Scheduling & Dispatch: A central calendar allows office staff to assign jobs to the nearest available worker based on GPS location.",
  "Client Communication Hub: In-app messaging and status updates keep clients informed, reducing calls to the office.",
];
---

<BaseLayout title="Case Study: Local Trades Platform" pageDescription="A Progressive Web App (PWA) designed for local trade businesses to manage jobs, quotes, and client communication.">
  <style>
    body {
      background-color: #050505;
    }
  </style>

  <main class="py-24 px-4 text-off-white">
    <div class="max-w-4xl mx-auto">
      
      <!-- Header -->
      <div class="text-center mb-16">
        <h1 class="text-5xl md:text-7xl font-bold clash-display" style="color: #e1fe3f;">Platform for Local Trades</h1>
        <p class="text-xl md:text-2xl text-gray-300 mt-4 max-w-3xl mx-auto">A Progressive Web App (PWA) designed for local trade businesses to manage jobs, quotes, and client communication.</p>
      </div>

      <!-- Main Image -->
      <div class="mb-16">
        <img src="/localtrades-full.webp" alt="Platform for Local Trades" class="w-full rounded-2xl shadow-2xl shadow-electric-yellow/10 border border-white/10">
      </div>

      <!-- Tech Stack -->
      <div class="flex justify-center gap-3 mb-16 flex-wrap">
        {techStack.map(tag => (
          <span class="bg-white/10 text-off-white/80 text-sm font-semibold px-4 py-2 rounded-full">{tag}</span>
        ))}
      </div>

      <!-- Main Content -->
      <div class="prose prose-invert prose-lg max-w-none mx-auto text-gray-300">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-16">
          <div>
            <h2 class="text-3xl font-bold clash-display text-off-white">The Problem</h2>
            <p>Local trade businesses (plumbers, electricians, etc.) often rely on messy paperwork and constant phone calls between the office and field workers. This creates quoting delays, scheduling conflicts, and a poor customer experience.</p>
          </div>
          <div>
            <h2 class="text-3xl font-bold clash-display text-off-white">The Solution</h2>
            <p>We built a mobile-first Progressive Web App (PWA) that acts as an all-in-one field service management tool. It's installable on any device and works offline, syncing data once a connection is available.</p>
            <ul class="mt-6 space-y-4">
              {keyFeatures.map(feature => (
                <li class="flex items-start">
                  <svg class="w-6 h-6 text-green-400 mr-3 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path></svg>
                  <span>{feature}</span>
                </li>
              ))}
            </ul>
          </div>
        </div>

        <div class="mt-16 text-center bg-white/5 rounded-2xl p-8 border border-white/10">
          <h2 class="text-3xl font-bold clash-display text-off-white">Business Impact</h2>
          <p class="text-xl text-gray-300 mt-2 max-w-2xl mx-auto">The app enabled field workers to send quotes <strong style="color: #e1fe3f">80% faster</strong> than the previous paper-based method, leading to higher quote acceptance rates.</p>
        </div>
      </div>

      <!-- CTA -->
       <div class="text-center mt-24 flex flex-col sm:flex-row items-center justify-center gap-6">
        <a href="/#work" class="group relative px-10 py-5 border border-white/30 rounded-full overflow-hidden inline-block transition-all text-white/80 hover:text-white hover:border-white font-medium text-lg">
          <span>← Back to Work</span>
        </a>
        <a href="/#contact" class="group relative px-10 py-5 border border-electric-yellow rounded-full overflow-hidden inline-block transition-all text-electric-yellow btn-glow font-bold text-lg">
          <span class="relative z-10 group-hover:text-brand-dark transition-colors duration-300">Discuss This Solution</span>
          <div class="absolute inset-0 bg-electric-yellow translate-y-full group-hover:translate-y-0 transition-transform duration-300 ease-out"></div>
        </a>
      </div>

    </div>
  </main>
</BaseLayout>
